# photo-metadata-replicate

Replicate metadata from one source item onto a set of targets: union list
fields (e.g. keywords), fill in scalar fields (e.g. caption, description)
only if present on the source, never clobber existing data.

Work in progress. See `ROADMAP.md` for the milestone plan.
